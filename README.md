# MindooDB Test App

Eine Expo-Mobilanwendung für iOS und Android.

## Voraussetzungen

- Node.js 20 (verwende `nvm use 20`)
- npm oder yarn
- Expo CLI (wird automatisch mit `npx` verwendet)

## Installation

```bash
npm install
```

## Entwicklung

```bash
# Starte den Expo Development Server
npm start

# Für iOS
npm run ios

# Für Android
npm run android

# Für Web
npm run web
```

## Projektstruktur

- `App.js` - Hauptkomponente der App
- `app.json` - Expo-Konfiguration
- `package.json` - Dependencies und Scripts
